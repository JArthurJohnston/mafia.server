const ConnectionPool = require('../src/APIs/ConnectionPool')

describe('ConnectionPool', () => {
    let connectionPool

    beforeEach(() => {
        connectionPool = new ConnectionPool()
    })

    describe('addConnection', () => {
        it('should add a new connection to the list with a uuid', () => {
            
        })
    })
})
